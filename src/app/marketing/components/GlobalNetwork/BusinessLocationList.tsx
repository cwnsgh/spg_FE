"use client";

/**
 * 사업장 목록 UI 스텁(빈 컨테이너). 현재 프로젝트 내 다른 파일에서 import되지 않음.
 */
import styles from "./BusinessLocationList.module.css";

export default function BusinessLocationList({}) {
  return <div className={styles.container}></div>;
}
